<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.breakingbadapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="BBApp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BreakingBadAppNative"
        android:hardwareAccelerated="false">
        <activity
            android:name=".activities.detail.DetailActivity"
            android:exported="true" />
        <activity
            android:name="com.example.breakingbadappnative.activities.main.FirstMain"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.breakingbadappnative.activities.menuActivity"
            android:exported="true" />
        <activity
            android:name=".activities.main.MainActivity"
            android:exported="true" />
    </application>

</manifest>